# policy_diff_v1

`policy_diff_v1` compares `paper_policy_verdict_v1` and `production_policy_verdict_v1` produced from the same shared evidence DAG.

It exists for operator trust and must answer:
- can paper trade
- can production trade
- which blockers remain open only for production
