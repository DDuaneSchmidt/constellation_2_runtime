{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "C2_EXECUTION_EVIDENCE_LATEST_POINTER_V1",
  "title": "C2 Execution Evidence Latest Pointer v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_id",
    "schema_version",
    "produced_utc",
    "day_utc",
    "producer",
    "status",
    "reason_codes",
    "pointers"
  ],
  "properties": {
    "schema_id": { "const": "C2_EXECUTION_EVIDENCE_LATEST_POINTER_V1" },
    "schema_version": { "type": "integer", "const": 1 },

    "produced_utc": { "type": "string", "minLength": 1 },
    "day_utc": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$" },

    "producer": {
      "type": "object",
      "additionalProperties": false,
      "required": ["repo", "git_sha", "module"],
      "properties": {
        "repo": { "type": "string", "minLength": 1 },
        "git_sha": { "type": "string", "pattern": "^[0-9a-f]{7,40}$" },
        "module": { "type": "string", "minLength": 1 },
        "build": { "type": "object" }
      }
    },

    "status": {
      "type": "string",
      "enum": [
        "OK",
        "DEGRADED_MISSING_EXECUTION_EVENT",
        "FAIL_CORRUPT_INPUTS",
        "FAIL_SCHEMA_VIOLATION"
      ]
    },

    "reason_codes": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    },

    "pointers": {
      "type": "object",
      "additionalProperties": false,
      "required": ["submissions_day_dir", "submissions_day_sha256"],
      "properties": {
        "submissions_day_dir": { "type": "string", "minLength": 1 },
        "submissions_day_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
      }
    }
  }
}
