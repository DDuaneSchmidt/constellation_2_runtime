{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "c2.reports.delta_order_plan.v1",
  "title": "C2 Delta Order Plan v1 (Convergence Plan, Audit-Proof)",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_id",
    "schema_version",
    "day_utc",
    "produced_utc",
    "producer",
    "status",
    "reason_codes",
    "input_manifest",
    "mode",
    "orders",
    "plan_sha256"
  ],
  "properties": {
    "schema_id": { "type": "string", "const": "delta_order_plan" },
    "schema_version": { "type": "string", "const": "v1" },
    "day_utc": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$" },
    "produced_utc": { "type": "string", "minLength": 1 },
    "producer": {
      "type": "object",
      "additionalProperties": false,
      "required": ["repo", "module", "git_sha"],
      "properties": {
        "repo": { "type": "string", "minLength": 1, "maxLength": 128 },
        "module": { "type": "string", "minLength": 1, "maxLength": 256 },
        "git_sha": { "type": "string", "minLength": 7, "maxLength": 64 }
      }
    },
    "status": { "type": "string", "enum": ["OK", "FAIL", "DEGRADED"] },
    "reason_codes": { "type": "array", "items": { "type": "string", "minLength": 1, "maxLength": 128 } },
    "mode": { "type": "string", "enum": ["NORMAL", "REDUCE_ONLY", "FLATTEN_ONLY"] },
    "input_manifest": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["type", "path", "sha256"],
        "properties": {
          "type": { "type": "string", "minLength": 1, "maxLength": 128 },
          "path": { "type": "string", "minLength": 1, "maxLength": 1024 },
          "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" }
        }
      }
    },
    "orders": {
      "type": "array",
      "items": { "$ref": "#/$defs/order_item" }
    },
    "plan_sha256": {
      "type": "string",
      "description": "sha256 of canonical JSON for this document, excluding this field"
    }
  },
  "$defs": {
    "order_item": {
      "type": "object",
      "additionalProperties": false,
      "required": ["engine_id", "underlying", "action", "delta_notional_pct", "reason_codes"],
      "properties": {
        "engine_id": { "type": "string", "minLength": 1, "maxLength": 128 },
        "underlying": { "type": "string", "minLength": 1, "maxLength": 32 },
        "action": { "type": "string", "enum": ["INCREASE", "DECREASE", "CLOSE", "HOLD"] },
        "delta_notional_pct": { "type": "string", "pattern": "^-?(0(\\.[0-9]+)?|1(\\.0+)?)$" },
        "delta_notional_cents": { "anyOf": [{ "type": "integer" }, { "type": "null" }] },
        "reason_codes": { "type": "array", "items": { "type": "string", "minLength": 1, "maxLength": 128 } },
        "notes": { "type": "array", "items": { "type": "string", "minLength": 1, "maxLength": 512 } }
      }
    }
  }
}
