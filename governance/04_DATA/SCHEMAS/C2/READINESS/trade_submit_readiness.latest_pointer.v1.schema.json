{
  "$id": "C2_READINESS_TRADE_SUBMIT_READINESS_LATEST_POINTER_SCHEMA_V1",
  "title": "C2 Trade Submit Readiness (C2-native) — latest_pointer.v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_id",
    "schema_version",
    "day_utc",
    "as_of_utc",
    "expires_utc",
    "ok",
    "state",
    "environment",
    "ib_account",
    "target_path",
    "target_sha256",
    "producer",
    "provenance"
  ],
  "properties": {
    "schema_id": { "type": "string", "const": "trade_submit_readiness_c2_latest_pointer" },
    "schema_version": { "type": "string", "const": "v1" },
    "day_utc": { "type": "string", "minLength": 10, "maxLength": 10 },

    "as_of_utc": { "type": "string" },
    "expires_utc": { "type": "string" },

    "ok": { "type": "boolean" },
    "state": { "type": "string", "enum": ["OK", "FAIL"] },
    "environment": { "type": "string", "enum": ["PAPER", "LIVE"] },
    "ib_account": { "type": "string", "minLength": 1 },

    "target_path": { "type": "string" },
    "target_sha256": { "type": "string" },

    "producer": {
      "type": "object",
      "additionalProperties": false,
      "required": ["repo", "module", "git_sha"],
      "properties": {
        "repo": { "type": "string" },
        "module": { "type": "string" },
        "git_sha": { "type": "string" }
      }
    },

    "provenance": {
      "type": "object",
      "additionalProperties": false,
      "required": ["truth_root"],
      "properties": {
        "truth_root": { "type": "string" }
      }
    }
  }
}
