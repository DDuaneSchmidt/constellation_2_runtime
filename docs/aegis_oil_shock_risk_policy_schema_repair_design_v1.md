# Oil Shock Risk Policy Schema Repair Design v1

The design is schema-only: add `C2_OIL_SHOCK_REVERSAL_V1` to the risk policy registry schema policy-key allowlist and produce a read-only proof artifact. The registry policy values are not modified.
