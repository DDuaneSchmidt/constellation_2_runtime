[Unit]
Description=DEPRECATED non-owning C2 IB Gateway marker

[Service]
Type=oneshot
ExecStart=/usr/bin/printf 'DEPRECATED: c2-ib-gateway.service is non-owning. Aegis Lite is MANUAL_ONLY; IB Gateway is optional operator-started tooling only and is not Lite readiness authority.\n'

[Install]
WantedBy=default.target
