# Constellation 2.0 - repo hygiene (audit-grade)
# Rule: ignore build artifacts, virtualenvs, caches, and local outputs.
#
# IMPORTANT (C2 authority):
# - Runtime truth under constellation_2/runtime/truth/** is the authoritative immutable output surface
#   and MUST be trackable in git for reproducible, hash-verifiable replay.
# - Non-truth runtime outputs remain local by default.

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environments
.venv/
venv/
constellation_2/.venv/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS/editor
.DS_Store
*.swp
*.swo

# Runtime outputs
# Default: ignore everything under constellation_2/runtime/
constellation_2/runtime/
constellation_2/runtime/**

# EXCEPTION: canonical immutable truth MUST be trackable
!constellation_2/runtime/
!constellation_2/runtime/truth/
!constellation_2/runtime/truth/**
!constellation_2/runtime/truth/**/**

# Operator/local inputs used for runs (samples belong in docs or committed fixtures; live inputs are local)
constellation_2/phaseF/**/inputs/
constellation_2/phaseF/**/inputs/**

# Phase D local outputs (Phase D directories are governed, but outputs inside are not committed)
constellation_2/phaseD/outputs/
