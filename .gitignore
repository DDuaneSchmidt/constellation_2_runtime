# Constellation 2.0 - repo hygiene (audit-grade)
# Rule: ignore build artifacts, virtualenvs, caches, and local outputs.

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environments
.venv/
venv/
constellation_2/.venv/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS/editor
.DS_Store
*.swp
*.swo

# Local outputs (Phase D directories are governed, but outputs inside are not committed)
constellation_2/phaseD/outputs/
